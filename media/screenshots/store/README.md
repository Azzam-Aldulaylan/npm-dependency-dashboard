# Store screenshots

Captured from the running extension on 12 September 2026 using the existing frontend project with the workspace display name **Web application**. The project folder and manifest were not renamed.

Suggested gallery order:

1. `dashboard.png`: project health, update breakdown, and dependency inventory.
2. `cleanup-plan.png`: Smart Cleanup summary and evidence categories.
3. `upgrade-review.png`: completed compatibility and security-impact review.
4. `vulnerabilities.png`: advisory details and dependency paths.
5. `usage-references.png`: source and configuration references.

Images are cropped to extension content only, with the pointer parked outside the captured area. No findings, counts, or status messages were altered. Partial-check warnings remain visible. These are point-in-time findings, not current package-version or vulnerability claims.

No upgrade or cleanup was applied to generate these captures. A completion report was deliberately not staged. The cleanup confirmation capture was excluded because it displayed full local file paths.
